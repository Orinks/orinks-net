{
  "buildCommand": "if [ -z \"$CONVEX_DEPLOY_KEY\" ]; then npm run build; elif [ \"$VERCEL_GIT_COMMIT_REF\" = \"dev\" ]; then npx convex deploy --check-build-environment disable --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL --cmd \"npm run build\"; else npx convex deploy --cmd-url-env-var-name NEXT_PUBLIC_CONVEX_URL --cmd \"npm run build\"; fi"
}
